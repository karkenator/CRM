import React from 'react';

const Logo: React.FC = () => (
  <div className="flex items-center w-28 h-auto">
    <img
      src="/logo-light-mode.png"
      alt="AcesMaster"
      className="w-full h-auto object-contain"
    />
  </div>
);

export default Logo;
